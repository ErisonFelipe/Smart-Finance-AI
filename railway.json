{
  "build": {
    "builder": "nixpacks",
    "config": {
      "installCommand": "cd server && npm install",
      "buildCommand": "cd server && npx prisma generate",
      "startCommand": "cd server && npx prisma migrate deploy && node prisma/seed.js && node server.js"
    }
  }
}